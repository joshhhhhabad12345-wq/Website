--[[
    VDAMBOT White
    Advanced Camera Lock Script – Mobile Ready
    by VDAMBOT
--]]

local Players          = game:GetService('Players')
local RunService       = game:GetService('RunService')
local TweenService     = game:GetService('TweenService')
local UserInputService = game:GetService('UserInputService')
local GuiService       = game:GetService('GuiService')

local LocalPlayer = Players.LocalPlayer

-- ──────────────────────────────────────────────
--  Settings
-- ──────────────────────────────────────────────
local Settings = {
    Enabled    = false,
    Smoothness = 0.18,
    Prediction = 0.16,
    AutoSnap   = false,
    FOVVisible = true,
    FOVRadius  = 120,
    TargetPart = "HumanoidRootPart",
    Key        = Enum.KeyCode.C,
    AimColor   = Color3.fromRGB(255, 255, 255),
    LockMode   = "Camera",   -- "Camera" | "HRP" | "Both"
}

-- ──────────────────────────────────────────────
--  ESP – Flashing White Highlight
-- ──────────────────────────────────────────────
local ESPHighlight = nil
local flashThread  = nil

local function ClearESP()
    if flashThread then task.cancel(flashThread) flashThread = nil end
    if ESPHighlight then ESPHighlight:Destroy() ESPHighlight = nil end
end

local function ApplyESP(character)
    ClearESP()
    if not character then return end

    ESPHighlight = Instance.new("Highlight")
    ESPHighlight.FillColor            = Color3.fromRGB(255, 255, 255)
    ESPHighlight.OutlineColor         = Color3.fromRGB(255, 255, 255)
    ESPHighlight.FillTransparency     = 0.6
    ESPHighlight.OutlineTransparency  = 0
    ESPHighlight.DepthMode            = Enum.HighlightDepthMode.AlwaysOnTop
    ESPHighlight.Adornee              = character
    ESPHighlight.Parent               = character

    -- Flash loop: bright ↔ dim
    local tiIn  = TweenInfo.new(0.28, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut)
    local tiOut = TweenInfo.new(0.28, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut)
    flashThread = task.spawn(function()
        while ESPHighlight and ESPHighlight.Parent do
            TweenService:Create(ESPHighlight, tiIn,  {FillTransparency = 0.1,  OutlineTransparency = 0  }):Play()
            task.wait(0.28)
            TweenService:Create(ESPHighlight, tiOut, {FillTransparency = 0.88, OutlineTransparency = 0.5}):Play()
            task.wait(0.28)
        end
    end)
end

-- ──────────────────────────────────────────────
--  Target Management
-- ──────────────────────────────────────────────
local currentTarget = nil

local function SetTarget(part)
    currentTarget = part
    ApplyESP(part and part.Parent or nil)
end

-- ──────────────────────────────────────────────
--  Utility
-- ──────────────────────────────────────────────
local function ScreenCenter()
    local res = GuiService:GetScreenResolution()
    return Vector2.new(res.X / 2, res.Y / 2)
end

-- Find enemy nearest to a given screen-space point (default = screen center)
local function FindNearestToPoint(screenPoint)
    screenPoint = screenPoint or ScreenCenter()
    local closest, closestDist = nil, math.huge
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local char = p.Character
            local part = char:FindFirstChild(Settings.TargetPart)
            local hum  = char:FindFirstChildOfClass("Humanoid")
            if part and hum and hum.Health > 0 then
                local screenPos, onScreen = workspace.CurrentCamera:WorldToViewportPoint(part.Position)
                if onScreen then
                    local dist = (screenPoint - Vector2.new(screenPos.X, screenPos.Y)).Magnitude
                    if dist < closestDist then
                        closest, closestDist = part, dist
                    end
                end
            end
        end
    end
    return closest, closestDist
end

local function FindNearest()
    local part, dist = FindNearestToPoint(ScreenCenter())
    if dist and dist < Settings.FOVRadius then return part end
    return nil
end

-- ──────────────────────────────────────────────
--  Heartbeat – Lock Logic
-- ──────────────────────────────────────────────
RunService.Heartbeat:Connect(function()
    if not (Settings.Enabled and currentTarget) then return end

    -- Guard: target may have died or left
    local hum = currentTarget.Parent and currentTarget.Parent:FindFirstChildOfClass("Humanoid")
    if not hum or hum.Health <= 0 then SetTarget(nil) return end

    local cam      = workspace.CurrentCamera
    local velocity = currentTarget.Velocity or Vector3.zero
    local aimPos   = currentTarget.Position + velocity * Settings.Prediction
    local mode     = Settings.LockMode

    if mode == "Camera" or mode == "Both" then
        local targetCF = CFrame.new(cam.CFrame.Position, aimPos)
        if Settings.AutoSnap then
            cam.CFrame = targetCF
        else
            cam.CFrame = cam.CFrame:Lerp(targetCF, Settings.Smoothness)
        end
    end

    if mode == "HRP" or mode == "Both" then
        local char = LocalPlayer.Character
        local hrp  = char and char:FindFirstChild("HumanoidRootPart")
        if hrp then
            local flatTarget = Vector3.new(aimPos.X, hrp.Position.Y, aimPos.Z)
            local targetHRP  = CFrame.new(hrp.Position, flatTarget)
            if Settings.AutoSnap then
                hrp.CFrame = targetHRP
            else
                hrp.CFrame = hrp.CFrame:Lerp(targetHRP, Settings.Smoothness)
            end
        end
    end
end)

-- ──────────────────────────────────────────────
--  Keyboard Toggle (PC)
-- ──────────────────────────────────────────────
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Settings.Key then
        Settings.Enabled = not Settings.Enabled
        if Settings.Enabled then
            SetTarget(FindNearest())
        else
            SetTarget(nil)
        end
    end
end)

-- ──────────────────────────────────────────────
--  Touch-to-Target (Mobile)
--  Tap anywhere on screen → pick closest player to tap → lock on
-- ──────────────────────────────────────────────
local TAP_TOLERANCE = 200   -- pixels — generous for mobile finger taps

UserInputService.TouchTap:Connect(function(touch, gpe)
    if gpe then return end   -- ignore taps that landed on a GUI element
    if not Settings.Enabled then return end  -- only pick target when aimbot is on

    local tapPos  = Vector2.new(touch.Position.X, touch.Position.Y)
    local part, dist = FindNearestToPoint(tapPos)

    if part and dist <= TAP_TOLERANCE then
        SetTarget(part)
    end
end)

-- ──────────────────────────────────────────────
--  ScreenGui
-- ──────────────────────────────────────────────
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name           = "VDAMBOT_White"
ScreenGui.ResetOnSpawn   = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent         = game.CoreGui

-- ─── FOV Circle ──────────────────────────────
local FOVDrawing = Drawing.new("Circle")
FOVDrawing.Visible   = Settings.FOVVisible
FOVDrawing.Radius    = Settings.FOVRadius
FOVDrawing.Color     = Settings.AimColor
FOVDrawing.Thickness = 1.5
FOVDrawing.Filled    = false
FOVDrawing.NumSides  = 64

RunService.RenderStepped:Connect(function()
    FOVDrawing.Position = ScreenCenter()
    FOVDrawing.Visible  = Settings.FOVVisible
    FOVDrawing.Radius   = Settings.FOVRadius
    FOVDrawing.Color    = Settings.AimColor
end)

-- ─── Top Bar ─────────────────────────────────
local TopBar = Instance.new("Frame")
TopBar.Name             = "TopBar"
TopBar.Parent           = ScreenGui
TopBar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
TopBar.BorderSizePixel  = 0
TopBar.Size             = UDim2.new(0, 220, 0, 36)
TopBar.Position         = UDim2.new(0.5, -110, 0, 16)
TopBar.ClipsDescendants = true
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 10)

local TopBarStroke = Instance.new("UIStroke", TopBar)
TopBarStroke.Color        = Color3.fromRGB(255, 255, 255)
TopBarStroke.Thickness    = 1.2
TopBarStroke.Transparency = 0.6

local Accent = Instance.new("Frame", TopBar)
Accent.Size             = UDim2.new(0, 60, 0, 2)
Accent.Position         = UDim2.new(0.5, -30, 1, -2)
Accent.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Accent.BorderSizePixel  = 0
Instance.new("UICorner", Accent).CornerRadius = UDim.new(1, 0)

local Logo = Instance.new("TextLabel", TopBar)
Logo.Size                   = UDim2.new(0, 160, 1, 0)
Logo.Position               = UDim2.new(0, 12, 0, 0)
Logo.BackgroundTransparency = 1
Logo.Font                   = Enum.Font.GothamBold
Logo.Text                   = "VDAMBOT White"
Logo.TextColor3             = Color3.fromRGB(255, 255, 255)
Logo.TextSize               = 13
Logo.TextXAlignment         = Enum.TextXAlignment.Left

local PanelBtn = Instance.new("TextButton", TopBar)
PanelBtn.Size             = UDim2.new(0, 32, 0, 32)
PanelBtn.Position         = UDim2.new(1, -36, 0, 2)
PanelBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
PanelBtn.BorderSizePixel  = 0
PanelBtn.Font             = Enum.Font.GothamBold
PanelBtn.Text             = "≡"
PanelBtn.TextColor3       = Color3.fromRGB(255, 255, 255)
PanelBtn.TextSize         = 18
Instance.new("UICorner", PanelBtn).CornerRadius = UDim.new(0, 6)

-- ─── Drag – Mouse + Touch ────────────────────
do
    local dragging, dragStart, startPos = false, nil, nil

    local function BeginDrag(pos)
        dragging  = true
        dragStart = pos
        startPos  = TopBar.Position
    end
    local function EndDrag()
        dragging = false
    end
    local function MoveDrag(pos)
        if not dragging then return end
        local delta = pos - dragStart
        TopBar.Position = UDim2.new(
            startPos.X.Scale, startPos.X.Offset + delta.X,
            startPos.Y.Scale, startPos.Y.Offset + delta.Y
        )
    end

    -- Mouse
    TopBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            BeginDrag(input.Position)
        end
    end)
    TopBar.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then EndDrag() end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then MoveDrag(input.Position) end
    end)

    -- Touch (mobile)
    TopBar.TouchStarted:Connect(function(touch)
        BeginDrag(touch.Position)
    end)
    TopBar.TouchEnded:Connect(function()
        EndDrag()
    end)
    TopBar.TouchMoved:Connect(function(touch)
        MoveDrag(touch.Position)
    end)
end

-- ─── Panel ───────────────────────────────────
local Panel = Instance.new("Frame")
Panel.Name              = "Panel"
Panel.Parent            = ScreenGui
Panel.BackgroundColor3  = Color3.fromRGB(14, 14, 14)
Panel.BorderSizePixel   = 0
Panel.Size              = UDim2.new(0, 220, 0, 0)
Panel.ClipsDescendants  = true
Instance.new("UICorner", Panel).CornerRadius = UDim.new(0, 10)

local PanelStroke = Instance.new("UIStroke", Panel)
PanelStroke.Color        = Color3.fromRGB(255, 255, 255)
PanelStroke.Thickness    = 1.2
PanelStroke.Transparency = 0.7

local PanelList = Instance.new("UIListLayout", Panel)
PanelList.Padding             = UDim.new(0, 8)
PanelList.SortOrder           = Enum.SortOrder.LayoutOrder
PanelList.HorizontalAlignment = Enum.HorizontalAlignment.Center

local PanelPad = Instance.new("UIPadding", Panel)
PanelPad.PaddingTop    = UDim.new(0, 10)
PanelPad.PaddingBottom = UDim.new(0, 10)
PanelPad.PaddingLeft   = UDim.new(0, 10)
PanelPad.PaddingRight  = UDim.new(0, 10)

RunService.RenderStepped:Connect(function()
    Panel.Position = UDim2.new(
        TopBar.Position.X.Scale,  TopBar.Position.X.Offset,
        TopBar.Position.Y.Scale,  TopBar.Position.Y.Offset + 40
    )
end)

-- ──────────────────────────────────────────────
--  Widget Helpers
-- ──────────────────────────────────────────────
local function SectionLabel(text, order)
    local lbl = Instance.new("TextLabel", Panel)
    lbl.LayoutOrder            = order
    lbl.Size                   = UDim2.new(1, 0, 0, 14)
    lbl.BackgroundTransparency = 1
    lbl.Font                   = Enum.Font.GothamSemibold
    lbl.Text                   = text
    lbl.TextColor3             = Color3.fromRGB(180, 180, 180)
    lbl.TextSize               = 10
    lbl.TextXAlignment         = Enum.TextXAlignment.Left
    return lbl
end

-- Returns (row, programmatic setter fn)
local function ToggleRow(labelText, order, defaultOn, callback)
    local row = Instance.new("Frame", Panel)
    row.LayoutOrder      = order
    row.Size             = UDim2.new(1, 0, 0, 28)
    row.BackgroundColor3 = Color3.fromRGB(26, 26, 26)
    row.BorderSizePixel  = 0
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 7)

    local lbl = Instance.new("TextLabel", row)
    lbl.Size                   = UDim2.new(1, -52, 1, 0)
    lbl.Position               = UDim2.new(0, 10, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Font                   = Enum.Font.Gotham
    lbl.Text                   = labelText
    lbl.TextColor3             = Color3.fromRGB(240, 240, 240)
    lbl.TextSize               = 11
    lbl.TextXAlignment         = Enum.TextXAlignment.Left

    local track = Instance.new("Frame", row)
    track.Size             = UDim2.new(0, 36, 0, 18)
    track.Position         = UDim2.new(1, -46, 0.5, -9)
    track.BackgroundColor3 = defaultOn and Color3.fromRGB(255,255,255) or Color3.fromRGB(60,60,60)
    track.BorderSizePixel  = 0
    Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame", track)
    knob.Size             = UDim2.new(0, 14, 0, 14)
    knob.Position         = defaultOn and UDim2.new(1,-16,0.5,-7) or UDim2.new(0,2,0.5,-7)
    knob.BackgroundColor3 = defaultOn and Color3.fromRGB(20,20,20) or Color3.fromRGB(200,200,200)
    knob.BorderSizePixel  = 0
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local state = defaultOn
    local ti    = TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

    local function SetState(on, fireCallback)
        state = on
        TweenService:Create(track, ti, {BackgroundColor3 = on and Color3.fromRGB(255,255,255) or Color3.fromRGB(60,60,60)}):Play()
        TweenService:Create(knob,  ti, {Position = on and UDim2.new(1,-16,0.5,-7) or UDim2.new(0,2,0.5,-7)}):Play()
        TweenService:Create(knob,  ti, {BackgroundColor3 = on and Color3.fromRGB(20,20,20) or Color3.fromRGB(200,200,200)}):Play()
        if fireCallback then callback(on) end
    end

    local btn = Instance.new("TextButton", row)
    btn.Size               = UDim2.new(1, 0, 1, 0)
    btn.BackgroundTransparency = 1
    btn.Text               = ""
    btn.ZIndex             = 5
    btn.MouseButton1Click:Connect(function() SetState(not state, true) end)

    return row, SetState
end

local function SliderRow(labelText, order, minVal, maxVal, defaultVal, displayFmt, callback)
    local wrap = Instance.new("Frame", Panel)
    wrap.LayoutOrder      = order
    wrap.Size             = UDim2.new(1, 0, 0, 44)
    wrap.BackgroundColor3 = Color3.fromRGB(26, 26, 26)
    wrap.BorderSizePixel  = 0
    Instance.new("UICorner", wrap).CornerRadius = UDim.new(0, 7)

    local lbl = Instance.new("TextLabel", wrap)
    lbl.Size                   = UDim2.new(1, -10, 0, 16)
    lbl.Position               = UDim2.new(0, 10, 0, 4)
    lbl.BackgroundTransparency = 1
    lbl.Font                   = Enum.Font.Gotham
    lbl.Text                   = labelText
    lbl.TextColor3             = Color3.fromRGB(210, 210, 210)
    lbl.TextSize               = 11
    lbl.TextXAlignment         = Enum.TextXAlignment.Left

    local valLbl = Instance.new("TextLabel", wrap)
    valLbl.Size                   = UDim2.new(0, 40, 0, 16)
    valLbl.Position               = UDim2.new(1, -50, 0, 4)
    valLbl.BackgroundTransparency = 1
    valLbl.Font                   = Enum.Font.GothamSemibold
    valLbl.Text                   = string.format(displayFmt, defaultVal)
    valLbl.TextColor3             = Color3.fromRGB(255, 255, 255)
    valLbl.TextSize               = 11
    valLbl.TextXAlignment         = Enum.TextXAlignment.Right

    local track = Instance.new("Frame", wrap)
    track.Size             = UDim2.new(1, -20, 0, 5)
    track.Position         = UDim2.new(0, 10, 0, 28)
    track.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    track.BorderSizePixel  = 0
    Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)

    local fill = Instance.new("Frame", track)
    fill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    fill.BorderSizePixel  = 0
    fill.Size             = UDim2.new((defaultVal - minVal) / (maxVal - minVal), 0, 1, 0)
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)

    local thumb = Instance.new("Frame", track)
    thumb.Size             = UDim2.new(0, 12, 0, 12)
    thumb.AnchorPoint      = Vector2.new(0.5, 0.5)
    thumb.Position         = UDim2.new((defaultVal - minVal) / (maxVal - minVal), 0, 0.5, 0)
    thumb.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    thumb.BorderSizePixel  = 0
    Instance.new("UICorner", thumb).CornerRadius = UDim.new(1, 0)

    -- Make a tall invisible hitbox easier to tap on mobile
    local hitbox = Instance.new("TextButton", wrap)
    hitbox.Size               = UDim2.new(1, -20, 0, 28)
    hitbox.Position           = UDim2.new(0, 10, 0, 16)
    hitbox.BackgroundTransparency = 1
    hitbox.Text               = ""
    hitbox.ZIndex             = 10

    local dragging = false

    local function UpdateFromX(mx)
        local abs  = track.AbsolutePosition
        local aw   = track.AbsoluteSize.X
        local frac = math.clamp((mx - abs.X) / aw, 0, 1)
        local val  = minVal + (maxVal - minVal) * frac
        fill.Size      = UDim2.new(frac, 0, 1, 0)
        thumb.Position = UDim2.new(frac, 0, 0.5, 0)
        valLbl.Text    = string.format(displayFmt, val)
        callback(val)
    end

    -- Mouse
    hitbox.MouseButton1Down:Connect(function()
        dragging = true
        UpdateFromX(UserInputService:GetMouseLocation().X)
    end)
    UserInputService.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if dragging and i.UserInputType == Enum.UserInputType.MouseMovement then
            UpdateFromX(i.Position.X)
        end
    end)

    -- Touch (mobile)
    hitbox.TouchStarted:Connect(function(touch)
        dragging = true
        UpdateFromX(touch.Position.X)
    end)
    hitbox.TouchEnded:Connect(function() dragging = false end)
    hitbox.TouchMoved:Connect(function(touch)
        if dragging then UpdateFromX(touch.Position.X) end
    end)

    return wrap
end

-- ─── Mode Picker ─────────────────────────────
local function ModePicker(order)
    local MODES = {
        { label = "Camera",  value = "Camera" },
        { label = "HRP",     value = "HRP"    },
        { label = "Cam+HRP", value = "Both"   },
    }
    local ACTIVE_BG   = Color3.fromRGB(255, 255, 255)
    local INACTIVE_BG = Color3.fromRGB(36,  36,  36 )
    local ACTIVE_TX   = Color3.fromRGB(10,  10,  10 )
    local INACTIVE_TX = Color3.fromRGB(190, 190, 190)

    local wrap = Instance.new("Frame", Panel)
    wrap.LayoutOrder      = order
    wrap.Size             = UDim2.new(1, 0, 0, 56)
    wrap.BackgroundColor3 = Color3.fromRGB(26, 26, 26)
    wrap.BorderSizePixel  = 0
    Instance.new("UICorner", wrap).CornerRadius = UDim.new(0, 7)

    local hdr = Instance.new("TextLabel", wrap)
    hdr.Size                   = UDim2.new(1, -10, 0, 16)
    hdr.Position               = UDim2.new(0, 10, 0, 4)
    hdr.BackgroundTransparency = 1
    hdr.Font                   = Enum.Font.Gotham
    hdr.Text                   = "Lock Mode"
    hdr.TextColor3             = Color3.fromRGB(210, 210, 210)
    hdr.TextSize               = 11
    hdr.TextXAlignment         = Enum.TextXAlignment.Left

    local seg = Instance.new("Frame", wrap)
    seg.Size             = UDim2.new(1, -20, 0, 26)
    seg.Position         = UDim2.new(0, 10, 0, 24)
    seg.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
    seg.BorderSizePixel  = 0
    Instance.new("UICorner", seg).CornerRadius = UDim.new(0, 6)
    Instance.new("UIStroke", seg).Color = Color3.fromRGB(60, 60, 60)

    local segLayout = Instance.new("UIListLayout", seg)
    segLayout.FillDirection = Enum.FillDirection.Horizontal
    segLayout.SortOrder     = Enum.SortOrder.LayoutOrder

    local cells = {}
    local ti    = TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)

    local function Activate(idx)
        for i, c in ipairs(cells) do
            local on = (i == idx)
            TweenService:Create(c, ti, {BackgroundColor3 = on and ACTIVE_BG or INACTIVE_BG}):Play()
            TweenService:Create(c, ti, {TextColor3       = on and ACTIVE_TX or INACTIVE_TX}):Play()
        end
        Settings.LockMode = MODES[idx].value
    end

    for i, mode in ipairs(MODES) do
        local cell = Instance.new("TextButton", seg)
        cell.LayoutOrder      = i
        cell.Size             = UDim2.new(1/3, 0, 1, 0)
        cell.BackgroundColor3 = i == 1 and ACTIVE_BG or INACTIVE_BG
        cell.BorderSizePixel  = 0
        cell.Font             = Enum.Font.GothamSemibold
        cell.Text             = mode.label
        cell.TextColor3       = i == 1 and ACTIVE_TX or INACTIVE_TX
        cell.TextSize         = 10
        if i == 1 or i == #MODES then
            Instance.new("UICorner", cell).CornerRadius = UDim.new(0, 6)
        end
        local idx = i
        cell.MouseButton1Click:Connect(function() Activate(idx) end)
        cells[i] = cell
    end
    return wrap
end

-- ─── Color Picker ────────────────────────────
local function ColorPickerRow(order, callback)
    local COLORS = {
        Color3.fromRGB(255, 255, 255),
        Color3.fromRGB(255,  80,  80),
        Color3.fromRGB(255, 200,  50),
        Color3.fromRGB( 80, 220, 120),
        Color3.fromRGB( 80, 160, 255),
        Color3.fromRGB(200,  80, 255),
        Color3.fromRGB(255, 140,   0),
    }

    local wrap = Instance.new("Frame", Panel)
    wrap.LayoutOrder      = order
    wrap.Size             = UDim2.new(1, 0, 0, 50)
    wrap.BackgroundColor3 = Color3.fromRGB(26, 26, 26)
    wrap.BorderSizePixel  = 0
    Instance.new("UICorner", wrap).CornerRadius = UDim.new(0, 7)

    local lbl = Instance.new("TextLabel", wrap)
    lbl.Size                   = UDim2.new(1, -10, 0, 16)
    lbl.Position               = UDim2.new(0, 10, 0, 4)
    lbl.BackgroundTransparency = 1
    lbl.Font                   = Enum.Font.Gotham
    lbl.Text                   = "FOV / Aim Color"
    lbl.TextColor3             = Color3.fromRGB(210, 210, 210)
    lbl.TextSize               = 11
    lbl.TextXAlignment         = Enum.TextXAlignment.Left

    local list = Instance.new("Frame", wrap)
    list.Size               = UDim2.new(1, -20, 0, 22)
    list.Position           = UDim2.new(0, 10, 0, 24)
    list.BackgroundTransparency = 1

    local rowLayout = Instance.new("UIListLayout", list)
    rowLayout.FillDirection = Enum.FillDirection.Horizontal
    rowLayout.Padding       = UDim.new(0, 6)
    rowLayout.SortOrder     = Enum.SortOrder.LayoutOrder

    local selectedStroke = nil
    for i, col in ipairs(COLORS) do
        local sw = Instance.new("TextButton", list)
        sw.LayoutOrder      = i
        sw.Size             = UDim2.new(0, 22, 0, 22)
        sw.BackgroundColor3 = col
        sw.BorderSizePixel  = 0
        sw.Text             = ""
        Instance.new("UICorner", sw).CornerRadius = UDim.new(1, 0)

        local stroke = Instance.new("UIStroke", sw)
        stroke.Color        = Color3.fromRGB(255, 255, 255)
        stroke.Thickness    = 0
        stroke.Transparency = 1

        local function Pick()
            if selectedStroke then
                TweenService:Create(selectedStroke, TweenInfo.new(0.15), {Thickness = 0, Transparency = 1}):Play()
            end
            TweenService:Create(stroke, TweenInfo.new(0.15), {Thickness = 2, Transparency = 0}):Play()
            selectedStroke = stroke
            callback(col)
        end

        sw.MouseButton1Click:Connect(Pick)
        sw.MouseEnter:Connect(function()
            TweenService:Create(sw, TweenInfo.new(0.12), {Size = UDim2.new(0,25,0,25)}):Play()
        end)
        sw.MouseLeave:Connect(function()
            TweenService:Create(sw, TweenInfo.new(0.12), {Size = UDim2.new(0,22,0,22)}):Play()
        end)
    end
    return wrap
end

-- ──────────────────────────────────────────────
--  Build Panel Contents
-- ──────────────────────────────────────────────
SectionLabel("AIMBOT", 1)

local _, SetAimToggle = ToggleRow("Cam Lock  [C]", 2, false, function(on)
    Settings.Enabled = on
    if on then SetTarget(FindNearest()) else SetTarget(nil) end
end)

ToggleRow("Auto Snap", 3, false, function(on)
    Settings.AutoSnap = on
end)

SectionLabel("LOCK MODE", 4)
ModePicker(5)

SectionLabel("SMOOTHNESS", 6)
SliderRow("Aim Smoothness", 7, 0.01, 0.5, Settings.Smoothness, "%.2f", function(v)
    Settings.Smoothness = v
end)

SectionLabel("PREDICTION", 8)
SliderRow("Prediction", 9, 0.0, 0.5, Settings.Prediction, "%.2f", function(v)
    Settings.Prediction = v
end)

SectionLabel("FOV", 10)
ToggleRow("Show FOV Circle", 11, true, function(on)
    Settings.FOVVisible = on
end)
SliderRow("FOV Radius", 12, 40, 400, Settings.FOVRadius, "%.0f px", function(v)
    Settings.FOVRadius = v
end)

SectionLabel("COLOR", 13)
ColorPickerRow(14, function(c)
    Settings.AimColor = c
end)

-- Spacer
local spacer = Instance.new("Frame", Panel)
spacer.LayoutOrder          = 99
spacer.Size                 = UDim2.new(1, 0, 0, 2)
spacer.BackgroundTransparency = 1

-- ─── Status Bar ──────────────────────────────
local StatusBar = Instance.new("Frame", Panel)
StatusBar.LayoutOrder      = 100
StatusBar.Size             = UDim2.new(1, 0, 0, 22)
StatusBar.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
StatusBar.BorderSizePixel  = 0
Instance.new("UICorner", StatusBar).CornerRadius = UDim.new(0, 6)

local StatusLbl = Instance.new("TextLabel", StatusBar)
StatusLbl.Size                   = UDim2.new(1, -8, 1, 0)
StatusLbl.Position               = UDim2.new(0, 8, 0, 0)
StatusLbl.BackgroundTransparency = 1
StatusLbl.Font                   = Enum.Font.GothamSemibold
StatusLbl.Text                   = "● INACTIVE"
StatusLbl.TextColor3             = Color3.fromRGB(130, 130, 130)
StatusLbl.TextSize               = 10
StatusLbl.TextXAlignment         = Enum.TextXAlignment.Left

RunService.Heartbeat:Connect(function()
    if Settings.Enabled and currentTarget then
        StatusLbl.Text       = "● LOCKED  [" .. Settings.LockMode .. "]"
        StatusLbl.TextColor3 = Color3.fromRGB(120, 255, 150)
    elseif Settings.Enabled then
        StatusLbl.Text       = "● SEARCHING…"
        StatusLbl.TextColor3 = Color3.fromRGB(255, 210, 80)
        SetTarget(FindNearest())
    else
        StatusLbl.Text       = "● INACTIVE"
        StatusLbl.TextColor3 = Color3.fromRGB(130, 130, 130)
    end
end)

-- ──────────────────────────────────────────────
--  Panel Open / Close
-- ──────────────────────────────────────────────
local PANEL_FULL_HEIGHT = 450
local panelOpen         = false
local panelTI           = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

local function SetPanelOpen(open)
    panelOpen = open
    TweenService:Create(Panel, panelTI, {Size = UDim2.new(0, 220, 0, open and PANEL_FULL_HEIGHT or 0)}):Play()
    TweenService:Create(PanelBtn, TweenInfo.new(0.2), {
        TextColor3 = open and Color3.fromRGB(255,255,255) or Color3.fromRGB(160,160,160)
    }):Play()
end

PanelBtn.MouseButton1Click:Connect(function() SetPanelOpen(not panelOpen) end)

PanelBtn.MouseEnter:Connect(function()
    TweenService:Create(PanelBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(55,55,55)}):Play()
end)
PanelBtn.MouseLeave:Connect(function()
    TweenService:Create(PanelBtn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(35,35,35)}):Play()
end)

-- ──────────────────────────────────────────────
--  Entrance Animation
-- ──────────────────────────────────────────────
TopBar.Position = UDim2.new(0.5, -110, 0, -50)
task.wait(0.1)
TweenService:Create(TopBar,
    TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out),
    {Position = UDim2.new(0.5, -110, 0, 16)}
):Play()

task.spawn(function()
    while true do
        TweenService:Create(Accent, TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {BackgroundTransparency = 0.7}):Play()
        task.wait(0.8)
        TweenService:Create(Accent, TweenInfo.new(0.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {BackgroundTransparency = 0}):Play()
        task.wait(0.8)
    end
end)

task.delay(0.6, function()
    game:GetService('StarterGui'):SetCore('SendNotification', {
        Title    = 'VDAMBOT White',
        Text     = 'Loaded!  Tap an enemy to target. Drag the bar to move.',
        Duration = 4,
    })
end)
